<IfModule mod_rewrite.c>
RewriteLogLevel 3
RewriteLog logs/rewrite.log
<Directory "/var/www/html">
    RewriteEngine on
    RewriteBase /assets
    RewriteRule ^images/(.*) $1 [L]
</Directory>
</IfModule>
